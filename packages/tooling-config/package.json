{
  "name": "tooling-config",
  "description": "A package used by every other package.",
  "scripts": {
    "build": "mkdir -p dist && cp src/index.html dist/index.html && echo \"tooling-config is built!\""
  }
}
